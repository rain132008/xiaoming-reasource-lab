# 开发记录

## 当前阶段

- 当前分支：`develop`
- 当前阶段：Cloudflare Pages + Pages Functions + D1 已接入
- 当前目标：使用 D1 作为资源数据源，前端页面统一通过 API 读取资源，不再读取本地 mock 数据
- 本地开发主地址：`http://localhost:8788`

## 已确认信息

- 项目名称：小明资料铺
- 前端技术栈：Vue 3 + Vite + Vue Router
- 部署方案：Cloudflare Pages
- API 方案：Cloudflare Pages Functions
- 数据库：Cloudflare D1
- 远程 D1 数据库名：`resource-share-db`
- 远程 D1 Database ID：`1254aeac-079c-4c26-bc6e-8e823db65399`
- Cloudflare Account ID：`e4809a9a44f835eb88958b288971fad3`

## 当前代码状态

- 首页已改为调用 `GET /api/resources` 获取资源列表
- 首页搜索已改为调用 `GET /api/search?q=...`
- 详情页已改为调用 `GET /api/resources/:slug`
- 详情页“免费获取资料”已改为调用 `POST /api/resources/:slug/access`
- `src/data/home.ts` 和 `src/data/resources.ts` 已删除
- 前端资源读取统一封装在 `src/services/resources.ts`
- 本地资料录入页不再进入正式前端路由
- 本地录入工具放在 `local-admin/resource-entry.html`，该目录已加入 `.gitignore`

## Cloudflare / D1

### Wrangler 配置

`wrangler.toml` 当前包含：

- `account_id`
- `pages_build_output_dir = "dist"`
- D1 绑定：`DB`
- D1 数据库：`resource-share-db`
- 迁移目录：`migrations`

### D1 表

当前 D1 包含 4 张业务表：

- `resources`：资源主表
- `click_logs`：访问和点击日志
- `search_logs`：搜索关键词日志
- `resource_requests`：资源心愿单 / 需求表

详细设计见：

- `Cloudflare-D1-数据库与接口设计.md`

### 迁移文件

- `migrations/0001_init.sql`
- `migrations/0002_seed_initial_resources.sql`

远程迁移已执行成功，远程 `resources` 当前有 4 条种子数据。

## API 状态

已实现：

```text
GET  /api/resources
GET  /api/resources/:slug
POST /api/resources/:slug/access
GET  /api/search?q=...
POST /api/requests
POST /api/resources
```

说明：

- 普通列表和详情接口不返回 `quark_url`
- `POST /api/resources/:slug/access` 会记录点击并返回真实资源链接
- `POST /api/resources` 用于录入资料，需要 `ADMIN_TOKEN`
- `POST /api/requests` 用于资源心愿单，后续可接 Turnstile

## 本地开发命令

安装依赖后：

```bash
npm run build
npm run cf:functions:build
```

本地跑 Cloudflare Pages Functions + D1：

```bash
npm run cf:dev
```

本地 D1 迁移：

```bash
npm run d1:migrate:local
```

远程 D1 迁移：

```bash
npm run d1:migrate:remote
```

如果切换过 `database_id`，本地 D1 目录会变，可能需要重新执行：

```bash
npm run d1:migrate:local
```

## 本地环境变量

本地 Cloudflare Pages dev 使用 `.dev.vars`：

```env
ADMIN_TOKEN=local-dev-admin-token
TURNSTILE_SECRET_KEY=
```

Cloudflare API Token 放在 `.env`：

```env
CLOUDFLARE_API_TOKEN=你的token
```

`.env`、`.dev.vars`、`.wrangler/`、`local-admin/` 都已加入忽略，不应提交。

## 本地录资料流程

详细说明见：

- `本地资料录入说明.md`

当前推荐流程：

1. 启动 `npm run cf:dev`
2. 打开 `local-admin/resource-entry.html`
3. 管理员口令填写 `.dev.vars` 中的 `ADMIN_TOKEN`
4. 提交后写入本地 D1
5. 本地确认无误后，再考虑同步到远程 D1

## 已验证

最近一次验证：

```bash
npm run build
npm run cf:functions:build
npm run d1:migrate:local
```

接口验证：

- `GET http://localhost:8788/api/resources`
- `GET http://localhost:8788/api/resources/xiaohongshu-title-templates`
- `GET http://localhost:8788/api/search?q=AI`
- `POST http://localhost:8788/api/resources/xiaohongshu-title-templates/access`

结果均正常。

## 注意事项

- `public/_redirects` 使用 `/* /index.html 200` 适配 Vue Router history 模式；Wrangler 本地会提示循环规则，但页面刷新和深链接可用。
- 生产环境需要在 Cloudflare Pages 配置 `ADMIN_TOKEN`，否则录入接口会返回 `ADMIN_TOKEN is not configured`。
- 录入接口仍然存在于 Functions 中，但录入页面只保留在本地 ignored 工具里，不进入线上前端。
- 前端目前依赖 API 数据，D1 没有表或没有迁移时，首页资源区会加载失败。

## 后续待办

- 给本地录入数据增加“导出到远程 D1”的脚本
- 前端增加资源心愿单页面和提交表单
- `POST /api/requests` 接入 Turnstile
- 增加资源分类筛选
- 增加基础数据看板：热门资源、搜索关键词、access 点击
- 录入工具后续可支持编辑已有资源和隐藏资源
