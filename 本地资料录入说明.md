# 本地资料录入说明

这个项目暂时不把录入页提交到线上前端。录资料使用本地工具：

```text
local-admin/resource-entry.html
```

`local-admin/` 已经加入 `.gitignore`，不会被提交。

## 1. 启动本地 Cloudflare Pages Dev

先确认项目根目录有 `.dev.vars`：

```env
ADMIN_TOKEN=local-dev-admin-token
TURNSTILE_SECRET_KEY=
```

然后启动：

```bash
npm run cf:dev
```

本地 API 地址是：

```text
http://localhost:8788
```

## 2. 打开录入工具

直接用浏览器打开：

```text
D:\ai\aiProj\reasource_share\local-admin\resource-entry.html
```

也可以在文件管理器里双击打开。

## 3. 填写管理员口令

本地默认口令：

```text
local-dev-admin-token
```

这个口令来自 `.dev.vars` 的 `ADMIN_TOKEN`。

生产环境不要用这个值，需要在 Cloudflare Pages 环境变量里设置一个新的长随机口令。

## 4. 字段填写规则

必填字段：

- `资料标题`
- `Slug`
- `分类`
- `一句话简介`
- `夸克分享链接`
- `管理员口令`

Slug 规则：

```text
只能使用小写字母、数字和短横线
```

示例：

```text
xiaohongshu-title-templates
ai-video-script-template
```

多行字段：

- `资料内容`：每行一条，会存成 JSON 数组
- `适合人群`：每行一条，会存成 JSON 数组
- `相关资料 slug`：每行一条，会存成 JSON 数组

标签字段：

```text
小红书, 标题, 自媒体
```

会存成：

```json
["小红书", "标题", "自媒体"]
```

## 5. 字段详细说明

### 管理员口令

对应请求头里的 `x-admin-token`。

本地开发时来自 `.dev.vars`：

```env
ADMIN_TOKEN=local-dev-admin-token
```

这个字段不入库，只用来证明这次录入是管理员操作。

### 资料标题

对应数据库字段：`resources.title`

展示位置：

- 首页资源卡片标题
- 详情页主标题
- 相关资料标题

建议写用户能直接搜索到的完整名称，例如：

```text
小红书标题模板 100 条
AI 短视频脚本模板
述职 PPT 结构模板
```

### Slug

对应数据库字段：`resources.slug`

Slug 是这份资料的 URL 标识，也就是详情页地址里最后那一段。

例如 slug 是：

```text
xiaohongshu-title-templates
```

那么详情页地址就是：

```text
http://localhost:8788/resource/xiaohongshu-title-templates
```

它的作用：

- 决定资源详情页 URL
- 用于外部平台分享链接
- 用于相关资料推荐里的互相引用
- 用于 access 接口获取资料链接
- 必须唯一，不能和已有资源重复

规则：

```text
只能使用小写英文字母、数字和短横线
```

推荐写法：

```text
主题关键词-资料类型
```

示例：

```text
xiaohongshu-title-templates
ai-video-script-template
excel-functions-cheatsheet
work-review-template
```

不推荐：

```text
小红书标题模板
XHS_Title_Template
xiaohongshu title templates
xiaohongshu_title_templates
```

如果 slug 填错了，后续最好新建一条正确 slug 的资源，再隐藏旧资源；第一版暂时没有编辑功能。

### 分类

对应数据库字段：`resources.category`

展示位置：

- 首页分类卡片
- 资源卡片标签
- 详情页标签
- 相关推荐补齐逻辑

首页会根据数据库里的分类自动分组，不再依赖写死分类。

建议先控制在这些大类里：

```text
AI 学习资料
自媒体运营
办公模板
副业工具
学习资料
考研资料
雅思备考
```

同一类目尽量不要写多个近似名称，例如不要同时出现：

```text
AI资料
AI 学习资料
AI工具
```

否则首页会被拆成多个分类卡片。

### 文件类型

对应数据库字段：`resources.file_type`

展示位置：

- 首页资源卡片
- 详情页资源标签

示例：

```text
PDF
DOCX
XLSX
PPTX
ZIP
```

### 文件大小

对应数据库字段：`resources.file_size`

只是展示文本，不参与计算。

示例：

```text
3.5 MB
21.3 MB
持续更新
```

### 排序权重

对应数据库字段：`resources.sort_order`

首页资源列表默认按：

```text
sort_order DESC -> updated_at DESC -> id DESC
```

也就是说数字越大，越靠前。

建议：

- 普通资源填 `0`
- 想置顶或重点推的资源填 `50`、`100`
- 暂时不想突出但仍发布的资源填 `-1`

### 一句话简介

对应数据库字段：`resources.description`

展示位置：

- 首页资源卡片摘要相关场景
- 详情页主介绍
- 搜索匹配字段

建议用一句话说明：

```text
适合谁 + 包含什么 + 能解决什么问题
```

示例：

```text
适合图文号和内容新手使用的标题模板，方便快速改写。
```

### 标签

对应数据库字段：`resources.tags`

录入方式：逗号分隔。

示例：

```text
小红书, 标题, 自媒体
```

用途：

- 首页热门搜索候选
- 搜索匹配字段
- 后续可用于相关推荐

### 整理者

对应数据库字段：`resources.author`

默认：

```text
小明资料铺
```

展示在首页资源卡片底部。

### 图标路径

对应数据库字段：`resources.cover_image`

目前推荐使用项目里已有的 SVG：

```text
/assets/icon-book-clean.svg
/assets/icon-ai-clean.svg
/assets/icon-headset-clean.svg
/assets/icon-cap-clean.svg
/assets/icon-report-clean.svg
/assets/icon-wish-clean.svg
```

展示位置：

- 详情页右侧预览图

首页资源卡片当前用分类文字色块，不直接使用这个图标。

### 状态

对应数据库字段：`resources.status`

可选：

```text
published
hidden
```

说明：

- `published`：正常展示，列表、搜索、详情都能访问
- `hidden`：隐藏，不在普通接口里出现

如果资料还没整理好，建议先选 `hidden`。

### 资料内容

对应数据库字段：`resources.content_summary`

录入方式：每行一条。

示例：

```text
爆款标题结构
常用开头模板
情绪型标题示例
标题避坑清单
```

展示位置：

- 详情页主介绍下方的小标签

### 适合人群

对应数据库字段：`resources.suitable_users`

录入方式：每行一条。

示例：

```text
小红书新手
图文创作者
自媒体运营
副业尝试者
```

当前页面暂时没有完整展示，先入库，后续详情页可以扩展。

### 相关资料 slug

对应数据库字段：`resources.related_slugs`

录入方式：每行一个 slug。

示例：

```text
ai-video-script-template
prompt-starter-handbook
office-ppt-templates
```

用途：

- 详情页相关资料优先展示这些资源
- 如果这里填的资源不存在，接口会自动用同分类或热门资源补齐

注意这里填的是 slug，不是标题。

### 夸克分享链接

对应数据库字段：`resources.quark_url`

示例：

```text
https://pan.quark.cn/s/5cbe470eb035
```

说明：

- 普通列表接口和详情接口不会返回这个字段
- 只有用户点击“免费获取资料”时，`access` 接口才返回
- 点击时会记录 `click_logs`，并增加资源的 `click_count`

## 6. 保存后检查

保存成功后页面会出现“查看详情页”链接。

也可以用接口检查：

```bash
curl http://localhost:8788/api/resources
```

或者查本地 D1：

```bash
npx wrangler d1 execute DB --local --command "SELECT slug,title,status FROM resources ORDER BY id DESC LIMIT 10;"
```

## 7. 录到远程 D1

如果要录入远程数据库，需要满足：

1. 代码已经部署到 Cloudflare Pages
2. Pages 项目已绑定 D1 数据库 `resource-share-db`
3. Cloudflare Pages 环境变量里设置了 `ADMIN_TOKEN`
4. 本地录入工具里的 API 地址需要改成线上域名

当前工具里写的是：

```js
const API_BASE = "http://localhost:8788";
```

要录线上时，改成类似：

```js
const API_BASE = "https://你的站点.pages.dev";
```

## 8. 注意事项

- 录入接口会校验 `x-admin-token`
- 普通列表和详情接口不会返回 `quark_url`
- 只有 `POST /api/resources/:slug/access` 会返回真实夸克链接
- 如果 slug 重复，会返回 `Slug already exists`
- 本地录入工具不会提交到 Git，后续可以按实际需要随便改
